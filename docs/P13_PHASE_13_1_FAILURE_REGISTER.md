# P13 Phase 13.1 — Test Failure Register

> Scope: Konnect Nex CRM / HRMS / Payroll / Tax / Projects — Phase 13.1 Release Stabilization & Regression Recovery  
> Note: Classifications follow `APPLICATION_DEFECT`, `TEST_DEFECT`, `OUTDATED_TEST`, `ENVIRONMENT_ISSUE`, `EXPECTED_BEHAVIOR`.

| Test | Domain | Failure (summary) | Classification | Root Cause (current hypothesis) | Fix (planned/actual) | Retest Command |
| ---- | ------ | ----------------- | -------------- | -------------------------------- | --------------------- | -------------- |
| `Tests\Feature\LeadVisibilityTest` | CRM – Lead visibility | All 8 tests pass (including bulk filtered actions only affecting visible leads). | EXPECTED_BEHAVIOR | Lead visibility chain (`BulkOperationsService → LeadChangeStatusBulkAction → AppliesLeadListingFilters → LeadVisibilityService`) behaves as designed for sales executives, managers and owners. | None required. Keep as regression sentinel. | `php artisan test --filter=LeadVisibilityTest` |
| `Tests\Feature\OpportunityDynamicMetadataFormTest` | CRM – Opportunities / Metadata | Behaves correctly when run after a clean `migrate:fresh --env=testing`, but can hit migration-state errors if executed against a partially-initialized test DB. | ENVIRONMENT_ISSUE | Sensitive to test DB lifecycle; must always be run against a freshly migrated `novacrm_testing` schema due to `RefreshDatabase` using `migrate:fresh` internally. | Ensure `php artisan migrate:fresh --env=testing` has just run before this suite; no functional defects observed once the schema is stable. | `php artisan test tests/Feature/OpportunityDynamicMetadataFormTest.php` |
| `Tests\Feature\LeadTest` | CRM – Leads (web) | All 19 tests now **pass** after running against a clean `novacrm_testing` schema. | EXPECTED_BEHAVIOR | Previous failures were due to a corrupted test DB; after `php artisan migrate:fresh --env=testing`, core lead CRUD, dashboard stats, and follow-up flows behave as intended. | None required. Keep as regression sentinel for lead workflows, follow-ups, and basic RBAC/tenant isolation on web. | `php artisan test tests/Feature/LeadTest.php` |
| `Tests\Feature\LeadSearchTest` | CRM – Leads / Search | All 4 tests now **pass**, covering web and API search behavior. | EXPECTED_BEHAVIOR | Previous failures were environment-related; with a stable `novacrm_testing` DB, lead search respects fields, phone normalization, filters, pagination, and tenant isolation. | None required. Keep as regression sentinel for search behavior across web and API. | `php artisan test tests/Feature/LeadSearchTest.php` |
| `Tests\Feature\LeadMetadataFilterBugfixTest` | CRM – Leads / Metadata filters | 7 tests failing (metadata filters after web form create, multiple filters, static + metadata filters, inactive fields, tenant isolation, backfill, pagination/sort); 1 test (invalid filter) passes. | APPLICATION_DEFECT | Lead index metadata filtering does not match the documented behavior (filters are not reliably applied/combined or preserved across pagination/sorting, and some tenant isolation cases are not honored), even with a clean `novacrm_testing` schema. | Investigate and fix the metadata query + projection behavior for `leads.index` (web index context) so that filters and sorting behave as asserted in these tests, then re-run this suite. | `php artisan test tests/Feature/LeadMetadataFilterBugfixTest.php` |
| `Tests\Feature\LeadImportDiagnosticsTest` | CRM – Leads / Imports | All 5 tests pass against `novacrm_testing`. | EXPECTED_BEHAVIOR | Import diagnostics pipeline, error reporting and queue integration are functioning as designed in the isolated test DB. | None required. Keep as regression sentinel. | `php artisan test tests/Feature/LeadImportDiagnosticsTest.php` |
| `Tests\Feature\LeadImportTemplateTest` | CRM – Leads / Templates | Excel template structure changed: the sample column previously used for "Preferred Country" now contains "Postal Code", while metadata lookups and instructions remain correct. | OUTDATED_TEST (fixed) | The template headers were updated (e.g. to surface Postal Code) but the test still asserted the older sample header text at a fixed cell. | Updated the test to expect "Postal Code" at that position while still asserting that metadata lookups (including "Preferred Country") and instructions sheets are present and correct. | `php artisan test tests/Feature/LeadImportTemplateTest.php` |
| `Tests\Feature\LeadImportTest` | CRM – Leads / Imports | All 12 tests pass (CSV/XLSX imports, metadata, duplicates, tenant isolation, HTTP workflow). | EXPECTED_BEHAVIOR | Lead import service, queue integration and tenant isolation are stable in the test DB. | None required. Keep as regression sentinel. | `php artisan test tests/Feature/LeadImportTest.php` |
| `Tests\Feature\LeadIntakeApiTest` | CRM – Leads / API intake | Functional behavior is correct (standard API envelope with `success/message/data/meta/errors[]`), but the invalid-payload test was asserting Laravel’s default validation error shape. | TEST_DEFECT (fixed) | The API intentionally wraps validation errors in a custom envelope; `assertJsonValidationErrors` was looking for the old `errors.field => []` structure and failing even though the new `errors[]` array is correct. | Updated the test to assert 422, then inspect the `errors` array and ensure that entries for `name`, `phone`, and `source` are present by their `field` keys. | `php artisan test tests/Feature/LeadIntakeApiTest.php` |

> This register is **live** for Phase 13.1. New failures from CRM, HRMS, Payroll, Tax/TDS, HRMS APIs, Projects, Security/RBAC and Tenant isolation suites should be appended here with one row per failing test (or tightly-related group), updated classification, and a linked fix PR / commit where applicable.

